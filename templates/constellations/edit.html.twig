{% extends 'dashboard/index.html.twig' %}

{% block title %}Modifier ma constellation{% endblock %}

{% block body %}
    <div class="page-header">
        <a href="{{ path('app_user_constellations') }}" class="back-button">
            <i class="fas fa-arrow-left"></i>
            <span>Retour</span>
        </a>

        <h1>Modifier ma constellation</h1>
    </div>

    {{ include('constellations/_form.html.twig', {
        'button_label': 'Mettre à jour'
    }) }}

    <div class="delete-container">
        {{ include('constellations/_delete_form.html.twig') }}
    </div>
{% endblock %}