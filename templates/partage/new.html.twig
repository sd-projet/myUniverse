{% extends 'dashboard/index.html.twig' %}

{% block title %}Publier un post{% endblock %}

{% block body %}

    <div class="page-header">
        <a href="{{ path('app_partage_index') }}" class="back-button">
            <i class="fas fa-arrow-left"></i>
            <span>Retour</span>
        </a>

        <h1>Publier un post</h1>
    </div>

    {{ include('partage/_form.html.twig') }}

{% endblock %}