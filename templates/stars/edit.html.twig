{% extends 'dashboard/index.html.twig' %}

{% block title %}Modifier ma star{% endblock %}

{% block body %}
    <div class="page-header">
        <a href="{{ path('app_user_stars') }}" class="back-button">
            <i class="fas fa-arrow-left"></i>
            <span>Retour</span>
        </a>

        <h1>Modifier ma star</h1>
    </div>

    {{ include('stars/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="delete-container">
        {{ include('stars/_delete_form.html.twig') }}
    </div>
{% endblock %}