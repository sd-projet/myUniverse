<form method="post" action="{{ path('app_stars_delete', {'id': star.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ star.id) }}">
    <button class="btn">Delete</button>
</form>
